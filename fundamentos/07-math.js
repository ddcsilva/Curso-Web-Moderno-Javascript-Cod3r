const raio = 5.6;
// PI é uma constante dentro de Math
// Pow é uma função de exponenciação (numero, valor elevado)
const area = Math.PI / Math.pow(raio, 2)

console.log(area);

// Math é um objeto
console.log(typeof Math);